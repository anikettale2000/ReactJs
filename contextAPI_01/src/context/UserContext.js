
import React from "react";

const UserContext = React.createContext(); // step-1 - Create a Context:

export default UserContext;

{/*
    The useContext hook in React is a powerful feature that allows you to share state and other values across your component tree without prop-drilling.
    useful for managing global state or themes, authentication data, and more in a React application.
*/}

